//If we try to run this code then the compiler will print an error as main method is missing in the code

public class test{
static{
print(10);
}
static void print (int x) {
System.out.println (x);
System.exit(0);
}
}